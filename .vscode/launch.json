{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "attach",  // 🔥 En vez de "launch", usamos "attach"
      "name": "Attach Debug NestJS",
      "port": 9229,  // Puerto por defecto de Node.js en modo debug
      "restart": true,
      "autoAttachChildProcesses": true
    }
  ]
}
